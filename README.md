# `@lucid-softworks/number-mean`

Compute the arithmetic mean of finite numbers.

```ts
import { mean } from "@lucid-softworks/number-mean";

mean([1, 2, 6]); // 3
```

See the exported types and tests for exact edge-case behavior.
